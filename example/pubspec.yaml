name: example
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

# Optional (default path: lib/gen/)
version_gen: 
  path: lib/gen/ 

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.7
  version_gen:
    path: ../

flutter:
  uses-material-design: true
